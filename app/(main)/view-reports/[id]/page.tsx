import React from 'react';
import ViewReportPage from '@/src/view/components/view-report-page';

const ViewReport = ({ params: { id } }: { params: { id: string } }) => {
    return <ViewReportPage />;
};

export default ViewReport;
